<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="tax_act_windows" model="ir.actions.act_window">
        <field name="name">Tax</field>
        <field name="res_model">hrms.jam_kerja</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem
        id="tax_menu"
        name="Tax"
        parent="payroll_menu"
        action="tax_act_windows"
        sequence="1"/>
</odoo>
